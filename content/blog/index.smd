---
.title = "Blog",
.date = @date("1990-01-01T00:00:00"),
.author = "Sample Author",
.layout = "blog.shtml",
.alternatives = [{ 
    .name = "rss",
    .layout = "blog.xml", 
    .output = "index.xml",
}],
.draft = false,
--- 

Example:

```ziggy
.alternatives = [{ 
    .name = "rss",
    .layout = "rss.xml", 
    .output = "index.xml",
}],
```

```sh
#!/bin/sh
echo "Today's date is:"
date
ls -l
```

[(btw syntax highlighting is done statically)]($text.attrs('small'))
